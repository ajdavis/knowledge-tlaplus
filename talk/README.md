# Talk: Reasoning About Knowledge in TLA+

UCLA, March 3, 2026.

## Prerequisites

- LaTeX with Beamer (`pdflatex`, `beamer.cls`)
- The indistinguishability graph PDFs must exist in `../raft/`, `../coordinated-attack/`, and `../muddy-children/` (generated by `analyze.py` and `muddy-children-knowledge-analysis.py`)

## Build

```
cd talk
pdflatex slides.tex && pdflatex slides.tex
```

Run twice to resolve cross-references.
